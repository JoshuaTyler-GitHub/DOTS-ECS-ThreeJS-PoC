// engine - components
import Component from '@engine/components/Component';

class Scale extends Component {
  constructor(scale = {}) {
    super();
    this.scale = scale;
  }
}
export default Scale;
